# ONEBOT CONF (LOCKED)
# ASCII-only; append-only; no duplicate keys.

# -------------------------
# BASIC
# -------------------------
enabled=1
controlGroupId=120363402608825006@g.us

# -------------------------
# STORAGE
# -------------------------
ticketStoreSpec=jsonstore:Fallback/tickets.json
ticketSeqDigits=7
ticketTtlMs=300000

# -------------------------
# BUFFER / ALBUM
# -------------------------
msgBufferMax=20
burstMs=1200

# -------------------------
# SENDING
# -------------------------
sendPrefer=sendout,outsend,send

# -------------------------
# UI / TEMPLATE
# -------------------------
ticketCardTemplateRel=ui/Fallback/ticketcard.txt

# -------------------------
# LOG
# -------------------------
moduleLog=1
bugLog=1
detailLog=0
traceLog=0


# Reply bypass policy (manual operator replies)
replyManualReply=1
replyAllowOutsideWindow=1
replyBypassRateLimit=0

# Forward AV policy (manual fallback from customer -> control group)
forwardAvManualReply=1
forwardAvAllowOutsideWindow=1
forwardAvBypassRateLimit=0
forwardAvRetryMax=3
forwardAvRetryBaseMs=1200
forwardAvRetryMaxMs=6000

# Forward media sender preference
forwardMediaSendPrefer=outsend,sendout,transport,send