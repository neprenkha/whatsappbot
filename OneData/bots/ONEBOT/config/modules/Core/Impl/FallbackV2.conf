# ONEBOT CONF (LOCKED)
# ASCII-only; append-only; no duplicate keys.

enabled=1
controlGroupId=120363402608825006@g.us

ticketType=fallback
ticketStoreSpec=jsonstore:Fallback/tickets

sendPrefer=outsend,sendout,transport

burstMs=2500
msgBufferMax=20

mediaTimeoutMs=120000
mediaRetryMs=900
sendSpacingMs=800

templateRel=bots/ONEBOT/config/ui/Fallback/ticketcard.txt

moduleLog=1
bugLog=1
detailLog=0
traceLog=1
