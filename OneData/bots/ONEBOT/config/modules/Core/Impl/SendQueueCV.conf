# SendQueue settings
serviceName=send
delayMs=800
maxQueue=2000
batchMax=30

# Outbound dedupe (prevents duplicate forwards/replies caused by duplicate inbound events)
dedupeMs=6000
dedupeMax=8000
dedupeLog=1
