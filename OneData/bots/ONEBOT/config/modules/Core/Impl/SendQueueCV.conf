# OneData/bots/ONEBOT/config/modules/Core/Impl/SendQueueCV.conf
# Purpose: SendQueue CV implementation config.

# CORE
enabled=1
globalConfRel=modules/Core/Impl/GlobalCV.conf

# LOGGING
moduleLog=1
bugLog=1
detailLog=0
traceLog=0

# SERVICE
serviceName=send
baseSend=outbox
defaultIsAuto=1
defaultManualReply=0

# LOOP
delayMs=800
tickMs=800
batchMax=5
maxQueue=2000
minGapMsPerChat=0
maxAttempts=5
retryDelayMs=800
deadMax=500

# DEDUPE
dedupeMs=6000
dedupeMax=8000
dedupeLog=1