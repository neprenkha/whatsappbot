# CONFIG: OneData/bots/ONEBOT/config/modules/Core/Impl/SendQueueCV.conf
# Purpose: Send queue entrypoint for outbound pipeline.

# CORE SWITCHES
enabled=1
globalConfRel=modules/Core/Impl/GlobalCV.conf

# SERVICE WIRING
serviceName=send
outboxServiceName=outbox

# QUEUE LIMITS
maxQueue=2000
dedupeMs=6000
dedupeMax=8000

# MODULE LOGGING TOGGLES
moduleLog=1
bugLog=1
detailLog=0
traceLog=0