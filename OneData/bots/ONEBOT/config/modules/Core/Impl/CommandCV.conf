# CommandCV (implementation config)
prefix=!
allowInDm=1
allowInGroups=1

# ASCII only (avoid emoji / unicode symbols in CMD)
unknownText=Unknown command. Use !help

# If enabled, unknown text replies only inside control group
unknownControlGroupOnly=0
controlGroupId=120363402608825006@g.us

# If enabled, unknown DM commands are not replied here (so Fallback can still open a ticket)
unknownPassthroughDm=1
